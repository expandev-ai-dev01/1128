/**
 * @component App
 * @summary Root application component with routing
 * @domain core
 * @type application-root
 * @category core
 */

import { AppRouter } from './router';

export const App = () => {
  return <AppRouter />;
};
